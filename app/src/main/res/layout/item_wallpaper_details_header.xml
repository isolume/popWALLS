<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <include
        android:id="@id/divider_layout"
        layout="@layout/item_divider" />

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="24.0dip"
        android:paddingTop="16.0dip"
        android:paddingEnd="24.0dip"
        android:paddingBottom="4.0sp">

        <TextView
            android:id="@id/section_title"
            style="@style/TextStyle.Frames.Headline6"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="4.0sp"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="@string/palette"
            android:textColor="?colorOnSurface" />

        <TextView
            android:id="@id/section_subtitle"
            style="@style/TextStyle.Frames.Caption"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="@string/tap_to_copy"
            android:textColor="?colorOnSurface" />
    </LinearLayout>
</LinearLayout>